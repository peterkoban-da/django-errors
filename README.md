# django-errors

## Übung 1

Wechsele auf den branch `error-1`.

```shell
git fetch
git switch error-1
git pull
```

Führe die Migrationen aus.

```shell
python manage.py migrate
```

**Wo liegt der Fehler?**
